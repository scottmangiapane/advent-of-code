import chalk from 'chalk';
import * as fs from 'fs';

const input = fs
    .readFileSync('day25/input.txt')
    .toString().trim().split('\n');

for (const line of input) {}

console.log('Part 1: ' + chalk.green());
console.log('Part 2: ' + chalk.green());